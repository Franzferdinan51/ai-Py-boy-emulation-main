## Skill Workshop

- added a Hermes-inspired skill workshop backend that projects agent skill drafts into deterministic OpenClaw-compatible `SKILL.md` artifacts
- added read-only workshop inspection routes plus explicit repo-local skill install routes without changing emulator mutation flow
- exposed workshop inspection through the generic MCP server and surfaced compact skill install controls on both frontend shells
